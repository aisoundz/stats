# Monday, August 10, 2026

*STATS GAMETIME daily journal. Generated from the commit history, the slate and the
game-night logs — not from recollection. `node host/journal.js 2026-08-10`*

| | |
|---|---|
| Commits | **41** |
| Builds deployed | — |
| Rooms on the slate | — |
| QA suites in the repo | 43 |

## What shipped

- `59f1d32` Build .18 - pin the pick-slip action bar so the way forward is always visible
- `aaa7199` Build .20 - Call It stops covering the nav, real Me screen
- `c47284d` Build .21 - lock bar follows the card on tall screens, demo Call It re-arms
- `bf92823` Build .22 - Call It never shares a screen with a quarter question
- `bebf0c9` Build .24 - Gametime tab: Call It gets a home, nav back on picks
- `93948a8` .25 — pick deck cannot be silently skipped; Gametime tab gets real content + practice Call It
- `07155c0` .26 - Stats tab, real Call It bank from the league feed, Trash Talk rename
- `86a2e3a` .27 - fix Call It repeat + stuck locked card; real Board screen; season layer
- `4bf95b2` Control Room: live Call It is retrospective and resolves on the clock
- `1ccd838` .28 - fix header overlap + Call It stuck on live screen; injury report everywhere
- `a8ba12d` .29 - fix court backdrop flash on refresh; admin build stamp; QA gate green
- `fb97e5f` Control Room: reveal button explains instead of going dead
- `96a21f5` Control Room: an unmarked answer voids that question instead of deadlocking the night
- `1417094` .30 - phone fixes: single-column deck under 380px, lock bar clears the nav on tall phones, bigger tap targets
- `b5820ff` Control Room: clear-the-test-data button so a rehearsal cannot block the real night
- `573efe2` Fix reset button: window wrapper recursed into itself and blew the stack
- `ba7a528` .31 - host panel no longer reachable by players
- `c83ed75` Visible version stamp on the Me tab and in the Control Room header
- `1ae664a` Fix button rows: a full-width btn was squeezing its neighbour
- `124fc3a` .33 - a nav tab can no longer destroy a live game (position vs view)
- `20bae48` .34 - version on the landing page where you can actually find it
- `9443f51` .35 - auto-resume, Play no longer restarts a night, nav stays up while waiting
- `2adbead` .36 - return bar only when a round needs answering, and never over content
- `f51b1c1` .37 - Gametime shows the live feed score instead of waiting on a host post
- `7c1cd4f` .38 - board shows one player and names the reason when it cannot read the room
- `869158c` .39 - Home says Continue with your score when a night is under way
- `58abb46` player .40 — resume rejoins the room (fixes empty board, dead trash talk, no pushed rounds)
- `0535a93` Game Night #6 — player .42 + admin .20: NY@IND config, full rosters, auto-settled prediction sheet, slimmer result card, way home
- `85a4f1d` player .45 — the Feed on Gametime, the rail, swipe, deep-cut variety, honest season ranks
- `2dbc662` player .46 — a save belongs to one night (two games in two days meant resuming inside yesterday)
- `4acc5f6` player .47 — season game logs: season highs, runs of games, head-to-head history in Deep Cuts
- `d3fab7c` player .48 — the warm-up: Gametime is alive before tip. No back arrow on tabs, no double scoreboard.
- `695bbf4` player .49 — Call It pacing: instant commit, compact receipt, sub-second resolve, streak heat. Questions about what happened earlier in the quarter.
- `1c8217a` admin .21 — live Call It now asks about the whole quarter, not just the last bucket
- `3a0cd4c` player .50 — a member's home is not a stranger's: the pitch retires once you have an account
- `a11ae06` player .51 + admin .22 — takeover moments, share any card, and a rebuilt quarter question set for NY@IND
- `2ba045c` player .52 + admin .23 — every question states its scope: "both teams combined" replaces "BOTH teams"
- `fd30347` admin .24 — a new question set now actually reaches the host: untouched rounds self-update, edited ones are named with a load button
- `32b7ace` player .53 + admin .25 — fix 36 invalid font shorthands the browser was silently discarding
- `d70aa28` player .54 + admin .26 — telemetry, guided host mode, Trash Talk joins the room, professional landing copy
- `fb0d09b` player .55 — ARENA: team colour from the feed, rolling digits, run bar and sentence, coloured feed edges

## Notes

_Anything worth keeping that the machine cannot see: what a player said, what felt
wrong, what to try next. Add it here by hand — the generator will never overwrite a
day that already has a file unless you pass --force._
