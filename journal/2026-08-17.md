# Monday, August 17, 2026

*STATS GAMETIME daily journal. Generated from the commit history, the slate and the
game-night logs — not from recollection. `node host/journal.js 2026-08-17`*

| | |
|---|---|
| Commits | **23** |
| Builds deployed | — |
| Rooms on the slate | — |
| QA suites in the repo | 43 |

## What shipped

- `3c4af93` Six sports, one engine — plus an overtime round that finally exists
- `be74ce0` .nojekyll — this site has never needed Jekyll, and Jekyll can fail it
- `7b1323d` Two checks were red for eleven nights and the product was fine both times
- `fe47d76` The gate can no longer hang, lose coverage, or lie about which sport it is
- `77ce470` A player lost 155 points to a twenty-second race, and nothing was logged
- `69f707b` The night's config gets a home in the database, and B26 finally gets a check
- `5da388d` Tonight's game can come from the database — inert until one rule ships
- `5011d18` Question banks for MLB, NFL, NHL and MLS — every line proven before anyone sees it
- `142e31a` Baseball, football and hockey are playable — and soccer stops contradicting itself
- `d0690b0` Firestore rules deploy from this machine, with no CLI login
- `36b8474` Game Night #12 moves to tonight — and the questions can be heard and answered out loud
- `3751b21` Voice, proven in a browser — and the runner pointed at tonight
- `8a83314` Voice in PRACTICE — which is how everybody will test it, and where it was broken
- `041f3cb` Voice: it can hear you now — three bugs, and a voice you can choose
- `d6c941e` Voice: it hears words the way people say them, and iPhone gets the tap it was asking for
- `b040b0d` Christyn Williams was unpickable — ordering by minutes must not delete anybody
- `54e5802` He said "two", got marked wrong, and was then read back the number he said
- `2c6d539` The microphone was being thrown away between every question — one fault, three faces
- `aaf1291` Both voice suites accept a bare filename — the trap was in my own tool
- `013a45b` GN13 is configured, the board ranks on the number it prints, and the voice check works
- `9328be5` B33 and B35: the caught lane stops being deleted, and a quarter is scored from its own numbers
- `9523d91` Overtime exists — but only on a night that went there
- `ee22b72` Housekeeping: a 960KB build artifact stops riding along, and the skill stops contradicting its own gate

## Notes written today

- `~/gamenight-logs/GN12-watch-notes.md`

## Notes

_Anything worth keeping that the machine cannot see: what a player said, what felt
wrong, what to try next. Add it here by hand — the generator will never overwrite a
day that already has a file unless you pass --force._
