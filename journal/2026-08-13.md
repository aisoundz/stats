# Thursday, August 13, 2026

*STATS GAMETIME daily journal. Generated from the commit history, the slate and the
game-night logs — not from recollection. `node host/journal.js 2026-08-13`*

| | |
|---|---|
| Commits | **23** |
| Builds deployed | **admin.45** → **admin.45** |
| Rooms on the slate | — |
| QA suites in the repo | 43 |

## What shipped

- `3d5b325` rules: bring the deployed Firestore ruleset into the repo
- `cfe8ce2` rules: the runner's plan document is owner-only, read and write
- `a46d9b9` admin.37: the resolver survives the copy out of the bank, and the autopilot stops borrowing another scope
- `e19392c` admin.38: one push path per round — the step card owns the next one, the cards keep a confirmed out-of-order path
- `57b0c1b` rules: the score cap was below what a night can score
- `ba4390a` admin.39: no out-of-order push before the night has started — there is no sequence to be out of yet
- `32ad1e0` admin.40: B13 — the Control Room posted 0-0 "Q1 in progress" before tip. Same bug I fixed in the runner and left here.
- `a425ebd` door.99: B14 — practice needs an account too, and the front-door copy no longer promises otherwise
- `994bdfc` B15: one shared scorer. The room and the runner both grade through AUTO.tally; the phone stops reporting its own total.
- `fd20982` host: the runner scores the room after it posts a key, through the shared tally
- `89234c1` B16: the caught lane reaches the total. It never did — the phone writing pts was hiding it.
- `7e81852` host: the runner carries the caught lane through the tally too
- `6227ed2` rules: the server owns pts, speed and roundsDone; the client lanes are bounded
- `071649d` B17: the score lanes are named once. Three copies meant a refresh could zero your own Caught It points.
- `fc88df4` B18: the breakdown adds up to the total. Caught It was inside the quarter bar AND on its own row.
- `f2f7d5d` B19 + B20: the sign-in intent survives the inbox, and a score that changes repaints the board
- `02e2572` B22: identity has one builder. Boot hand-built SB.me, skipped the broadcast, and the whole app showed signed-out to a signed-in player.
- `f992dd9` B23-B25: one Caught It lock window, the card announces only what it paid, and migration keeps the caught lane
- `95cb278` B26: nothing is deleted without a copy. Eight nights of room answers were lost to one button.
- `945dc02` rules: the night archive is owner-only, read and write
- `c9c0a59` B26b — the runner saves the night without being asked
- `0bc579c` admin.45 — say what the score panel actually does now
- `fd7a631` admin.45 — the last two places still using the old button names

## Notes

_Anything worth keeping that the machine cannot see: what a player said, what felt
wrong, what to try next. Add it here by hand — the generator will never overwrite a
day that already has a file unless you pass --force._
